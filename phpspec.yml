extensions:
    - PhpSpec\Extension\CodeCoverageExtension

suites:
  types:
    namespace: Madkom\Collection
    psr4_prefix: Madkom\Collection
    spec_path: %paths.config%/tests

knp.welldone.exclusion:
    - "*Controller"
    - "*Test"

code_coverage:
    output: tests/coverage/phpspec.cov
    format: php

formatter.name: pretty